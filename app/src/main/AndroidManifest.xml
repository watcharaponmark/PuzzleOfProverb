<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.puzzleofproverb">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainplayActivity" />
        <activity android:name=".MainBattleplayActivity" />
        <activity android:name=".Play" />
        <activity android:name=".GameOver" />
        <activity android:name=".PlayBattle1" />
        <activity android:name=".Player2" />
        <activity android:name=".PlayBattle2" />
        <activity android:name=".GameOverBattle"></activity>
    </application>

</manifest>